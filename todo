create a group
  nowjs.getGroup("blah")
apply to a group
  some_group.now.some_fn
add to group
  some_group.addUser @user.clientId

child.appendTo(parent)
e = $("<tag id='someid' class='someclass'>content</tag>")

----------------
add time stamps to deliveries
update delivery times
sort deliveries by time
set striped background colorsw
stylize delivery table
